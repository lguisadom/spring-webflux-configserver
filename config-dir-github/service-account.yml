application:
  config:
    description: Profile Default

server:
  port: 8083

spring:
  application:
    name: service-account
  cloud:
    config:
      profile: dev

  data:
    mongodb:
      database: 'accountdb'
      host: 'localhost'
      port: 27017

#Endpoints
config:
  base:
    customer:
      endpoint: 'http://localhost:8082/customerType/1'